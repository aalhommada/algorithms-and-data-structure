---
title: Maximum Average Subarray I
description: Find the highest-average window of fixed length k by sliding a window instead of recomputing sums
---

<ProblemMeta
  difficulty="easy"
  pattern="Sliding Window (fixed size)"
  tags={["Array", "Sliding Window"]}
  leetcode="https://leetcode.com/problems/maximum-average-subarray-i/"
  problemNumber={643}
/>

## Problem Statement

Given an integer array `nums` and an integer `k`, find the **contiguous subarray of exactly `k` elements** with the maximum average, and return that average.

```text
Input:  nums = [1, 12, -5, -6, 50, 3], k = 4
Output: 12.75
Why:    the window [12, -5, -6, 50] sums to 51 → 51 / 4 = 12.75

Input:  nums = [5], k = 1
Output: 5.00000
```

## Intuition

Because `k` is fixed, the average is just `sum / k` — and `k` never changes. So **maximising the average is exactly the same as maximising the sum**. Divide only once, at the very end.

The naive method walks every starting position and adds up `k` elements: `O(n × k)`. But look at what happens between two neighbouring windows:

```text
nums = [1, 12, -5, -6, 50, 3],  k = 4

window at 0:  [ 1, 12, -5, -6]        sum = 2
window at 1:      [12, -5, -6, 50]    sum = 51
```

Almost everything is shared. Only two elements differ.

<Callout type="info" title="The key insight">
Sliding a fixed window one step right means:

**subtract the element that fell off the left, add the element that entered on the right.**

Two operations, not `k`. Every element is added once and removed once across the whole run.
</Callout>

### Real-world analogy

A 7-day moving average of your step count. You never re-add all seven days — you drop the oldest day and add today.

## Approach

<Steps>

<Step>
### Build the first window

Sum `nums[0 … k-1]`. This is the only place you pay `O(k)`.
</Step>

<Step>
### Record it as the best so far
</Step>

<Step>
### Slide to the end

For each `i` from `k` to `n-1`: `window += nums[i] - nums[i - k]`.

`nums[i]` is entering the window and `nums[i - k]` is the one leaving it.
</Step>

<Step>
### Track the maximum sum, divide once

Return `best / k` as a floating-point number.
</Step>

</Steps>

### Watch it run

<ArrayViz
  title="nums = [1, 12, -5, -6, 50, 3], k = 4"
  intervalMs={1500}
  frames={[
    {
      values: [1, 12, -5, -6, 50, 3],
      states: ["active", "active", "active", "active", "idle", "idle"],
      vars: { window: 2, best: 2 },
      note: "Build the first window: 1 + 12 + (-5) + (-6) = 2. best = 2.",
    },
    {
      values: [1, 12, -5, -6, 50, 3],
      states: ["discard", "active", "active", "active", "active", "idle"],
      pointers: [
        { index: 0, label: "out" },
        { index: 4, label: "in" },
      ],
      vars: { window: 51, best: 51 },
      note: "Slide right: subtract 1 (leaving), add 50 (entering) → 2 - 1 + 50 = 51. New best.",
    },
    {
      values: [1, 12, -5, -6, 50, 3],
      states: ["discard", "discard", "active", "active", "active", "active"],
      pointers: [
        { index: 1, label: "out" },
        { index: 5, label: "in" },
      ],
      vars: { window: 42, best: 51 },
      note: "Slide again: subtract 12, add 3 → 51 - 12 + 3 = 42. Not better.",
    },
    {
      values: [1, 12, -5, -6, 50, 3],
      states: ["discard", "match", "match", "match", "match", "discard"],
      vars: { best: 51, answer: "51 / 4 = 12.75" },
      note: "End of array. The best window was [12, -5, -6, 50]. Divide once: 12.75.",
    },
  ]}
  caption="Each slide costs two operations no matter how large k is."
/>

## Solution

<CodeTabs>
<Tab label="Python">

```python
def find_max_average(nums: list[int], k: int) -> float:
    """Highest average over any contiguous subarray of exactly k elements."""
    window = sum(nums[:k])
    best = window

    for i in range(k, len(nums)):
        window += nums[i] - nums[i - k]  # add entering, drop leaving
        best = max(best, window)

    return best / k
```

</Tab>
<Tab label="TypeScript">

```typescript
function findMaxAverage(nums: number[], k: number): number {
  let window = 0;
  for (let i = 0; i < k; i++) {
    window += nums[i];
  }

  let best = window;

  for (let i = k; i < nums.length; i++) {
    window += nums[i] - nums[i - k]; // add entering, drop leaving
    best = Math.max(best, window);
  }

  return best / k;
}
```

</Tab>
</CodeTabs>

<Callout type="warn" title="Do not start `best` at zero">
`best = 0` is wrong for arrays that are entirely negative — `nums = [-1, -2]`, `k = 1` would return `0` instead of `-1`. Seed `best` with the first real window, or with negative infinity.
</Callout>

<Callout type="warn" title="Integer division in Python">
`51 // 4` is `12`, not `12.75`. Use a single slash (`/`) so the result is a float. In TypeScript all numbers are floating point, so `/` already does the right thing.
</Callout>

## Fixed vs. Variable Windows

This is the *fixed-size* sliding window: the width never changes, so the loop is a simple slide. The other flavour — the *variable-size* window — grows and shrinks based on a condition (for example "the longest substring with no repeats"), and uses two independent pointers with an inner `while` loop.

Recognising which kind a problem needs is most of the work:

| Clue in the problem | Window type |
|---|---|
| "subarray of size `k`" | Fixed |
| "longest/shortest subarray such that …" | Variable |

## Complexity Analysis

<Complexity time="O(n)" space="O(1)" />

- **Time** — `O(k)` to build the first window plus `O(n - k)` slides, which is `O(n)` overall. The naive version is `O(n × k)`; at `n = 10⁵`, `k = 10⁴` that is a billion operations versus a hundred thousand.
- **Space** — two numbers.

## Edge Cases

<Accordions>

<Accordion title="k equals the array length">
The first window is the entire array, the sliding loop never runs, and the answer is the overall average. Correct.
</Accordion>

<Accordion title="All negative numbers">
Handled, as long as `best` is seeded with the first window rather than `0` — see the warning above.
</Accordion>

<Accordion title="Floating-point precision">
LeetCode accepts answers within `10⁻⁵`. Summing integers and dividing exactly once (rather than averaging repeatedly) keeps the error far below that.
</Accordion>

<Accordion title="k = 1">
The window is a single element, so the answer is simply the maximum of the array. The code handles it without a special case.
</Accordion>

</Accordions>

## Related Problems

- [Running Sum of 1d Array](/docs/arrays/running-sum-of-1d-array) — prefix sums, the other way to answer range-sum questions fast
- [Best Time to Buy and Sell Stock](/docs/arrays/best-time-to-buy-and-sell-stock) — another single-pass running statistic
- [Longest Palindrome](/docs/strings/longest-palindrome) — counting inside a pass instead of rescanning
