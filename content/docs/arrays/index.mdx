---
title: Arrays
description: Two pointers, sliding windows, prefix sums and hash maps — the patterns behind most array problems
---

Arrays are where almost everything starts, and for good reason: they force you to think about **access patterns**. An array gives you `O(1)` random access but `O(n)` insertion and deletion, and nearly every technique below is a way of exploiting the first to avoid paying the second.

## What an Array Actually Looks Like

An array is **one continuous block of memory**, divided into equal-sized slots.

<ArrayDiagram
  title="nums = [2, 7, 11, 15, 3]"
  values={[2, 7, 11, 15, 3]}
  states={["idle", "idle", "active", "idle", "idle"]}
  pointers={[{ index: 2, label: "nums[2]" }]}
  caption="The number under each slot is its index. Indices start at 0, so the third item is nums[2]."
/>

Two things follow from "one continuous block", and between them they explain every array technique.

### Why reading any index is instant

The computer knows where the block begins and how wide each slot is. So it does not search — it calculates:

```text
address of nums[i]  =  start address  +  i × slot size
```

One multiplication and one addition, whatever `i` is. `nums[999999]` costs precisely what `nums[0]` costs. That is `O(1)` **random access** — "random" meaning *any position, in any order, at the same price*.

<Callout type="info" title="This is why indices start at 0">
The index is not a counter, it is a **distance from the start**. The first item is 0 slots away from the beginning, the second is 1 slot away.

Once you read `nums[i]` as *"i slots along"* rather than *"the i-th thing"*, off-by-one errors get noticeably rarer.
</Callout>

### Why inserting is expensive

The slots must stay adjacent with no gaps — that is what makes the address arithmetic work. So making room in the middle means physically moving everything after it.

<ArrayViz
  title="Inserting 9 at index 1"
  frames={[
    {
      values: [2, 7, 11, 15, 3],
      states: ["idle", "active", "idle", "idle", "idle"],
      pointers: [{ index: 1, label: "insert here" }],
      note: "We want 9 at index 1. But index 1 is occupied, and so is every slot after it.",
    },
    {
      values: [2, 7, 11, 15, 3, 3],
      states: ["idle", "idle", "idle", "idle", "compare", "compare"],
      note: "Start from the BACK. Copy 3 one slot right. Starting from the front would overwrite values before copying them.",
    },
    {
      values: [2, 7, 11, 15, 15, 3],
      states: ["idle", "idle", "idle", "compare", "compare", "done"],
      note: "Copy 15 right.",
    },
    {
      values: [2, 7, 11, 11, 15, 3],
      states: ["idle", "idle", "compare", "compare", "done", "done"],
      note: "Copy 11 right.",
    },
    {
      values: [2, 7, 7, 11, 15, 3],
      states: ["idle", "compare", "compare", "done", "done", "done"],
      note: "Copy 7 right. The slot at index 1 is finally free.",
    },
    {
      values: [2, 9, 7, 11, 15, 3],
      states: ["idle", "match", "done", "done", "done", "done"],
      note: "Write 9. Four values moved to insert one — and inserting at index 0 would have moved all five. That is O(n).",
    },
  ]}
  caption="Deleting is the same in reverse: everything after the gap shifts left."
/>

<Callout type="warn" title="The consequence you will actually meet">
Removing items from the front of a list inside a loop turns an `O(n)` algorithm into `O(n²)` — every removal quietly shifts the entire rest of the array.

Python's `list.pop(0)` and JavaScript's `Array.shift()` are exactly this trap. Use `collections.deque` or an index that moves instead. See [stacks and queues](/docs/stacks-queues).
</Callout>

### The trade, in one table

| Operation | Cost | Why |
|---|---|---|
| Read or write `nums[i]` | `O(1)` | Address arithmetic |
| Append at the end | `O(1)`* | Nothing to move |
| Insert or delete in the middle | `O(n)` | Everything after shifts |
| Insert or delete at the front | `O(n)` | Everything shifts |
| Search an unsorted array | `O(n)` | Must check each slot |
| Search a **sorted** array | `O(log n)` | [Binary search](/docs/searching) |

\* *Amortised.* The block occasionally fills up, and then the whole thing is copied to a bigger one. That copy is `O(n)`, but it happens rarely enough that the average stays `O(1)`.

Read that table as a set of instructions: **you have a fast operation and a slow one, so build solutions out of the fast one.** Every pattern below does exactly that — two pointers, sliding windows and prefix sums all read freely and never shift anything.

## The Four Patterns

Almost every array problem here is one of these.

### 1. Hash map — turn searching into looking up

When you find yourself writing a nested loop to find "the other half" of something, a hash map usually collapses it to one pass. The cost is `O(n)` memory.

- [Two Sum](/docs/arrays/two-sum) — the archetype
- [Contains Duplicate](/docs/arrays/contains-duplicate) — presence only, so a set suffices
- [Group Anagrams](/docs/arrays/group-anagrams) — keyed by a computed signature
- [Top K Frequent Elements](/docs/arrays/top-k-frequent-elements) — counting, then ranking

### 2. Two pointers — let a sorted (or symmetric) structure guide you

Two indices moving under a rule that provably discards possibilities. The hard part is always the proof that what you skip could not have been the answer.

- [Container With Most Water](/docs/arrays/container-with-most-water) — converging from both ends
- [Merge Sorted Array](/docs/arrays/merge-sorted-array) — filling from the back to avoid shifting
- [Remove Duplicates from Sorted Array II](/docs/arrays/remove-duplicates-from-sorted-array-ii) — a read pointer and a write pointer
- [Trapping Rain Water](/docs/arrays/trapping-rain-water) — two pointers plus running maximums

### 3. Prefix sums — precompute so range queries become subtraction

Build cumulative totals once, and any range question becomes `O(1)` arithmetic.

- [Running Sum of 1d Array](/docs/arrays/running-sum-of-1d-array) — the building block
- [Find Pivot Index](/docs/arrays/find-pivot-index) — total minus left gives right, for free
- [Product of Array Except Self](/docs/arrays/product-of-array-except-self) — prefix and suffix, with multiplication

### 4. Single-pass running state — carry the answer as you go

Keep one or two variables that summarise everything seen so far. Often a dynamic-programming table collapsed down to scalars.

- [Best Time to Buy and Sell Stock](/docs/arrays/best-time-to-buy-and-sell-stock) — a running minimum
- [Maximum Average Subarray I](/docs/arrays/maximum-average-subarray-i) — a fixed-size sliding window

## How to Recognise Which One

| The problem says… | Reach for |
|---|---|
| "find a pair / has this appeared before" | Hash map or set |
| "the array is sorted" | Two pointers or binary search |
| "sum of a subarray / range" | Prefix sums |
| "subarray of size `k`" | Fixed sliding window |
| "longest / shortest subarray such that…" | Variable sliding window |
| "in place, `O(1)` extra space" | Read and write pointers |
| "`k` most frequent / largest" | Counting, then bucket sort or a heap |

## All Problems

| Problem | Difficulty | Pattern |
|---|---|---|
| [Two Sum](/docs/arrays/two-sum) | Easy | Hash Map |
| [Contains Duplicate](/docs/arrays/contains-duplicate) | Easy | Hash Set |
| [Best Time to Buy and Sell Stock](/docs/arrays/best-time-to-buy-and-sell-stock) | Easy | Running Minimum |
| [Running Sum of 1d Array](/docs/arrays/running-sum-of-1d-array) | Easy | Prefix Sum |
| [Find Pivot Index](/docs/arrays/find-pivot-index) | Easy | Prefix Sum |
| [Maximum Average Subarray I](/docs/arrays/maximum-average-subarray-i) | Easy | Sliding Window |
| [Merge Sorted Array](/docs/arrays/merge-sorted-array) | Easy | Two Pointers |
| [Number of Good Pairs](/docs/arrays/number-of-good-pairs) | Easy | Counting |
| [Minimum Index Sum of Two Lists](/docs/arrays/minimum-index-sum-of-two-lists) | Easy | Hash Map |
| [Sort the People](/docs/arrays/sort-the-people) | Easy | Custom Sort |
| [Final Value of Variable](/docs/arrays/final-value-of-variable-after-performing-operations) | Easy | Simulation |
| [Container With Most Water](/docs/arrays/container-with-most-water) | Medium | Two Pointers |
| [Product of Array Except Self](/docs/arrays/product-of-array-except-self) | Medium | Prefix & Suffix |
| [Group Anagrams](/docs/arrays/group-anagrams) | Medium | Hash Map |
| [Top K Frequent Elements](/docs/arrays/top-k-frequent-elements) | Medium | Bucket Sort |
| [H-Index](/docs/arrays/h-index) | Medium | Sorting |
| [Remove Duplicates from Sorted Array II](/docs/arrays/remove-duplicates-from-sorted-array-ii) | Medium | Two Pointers |
| [Trapping Rain Water](/docs/arrays/trapping-rain-water) | Hard | Two Pointers |

<Callout type="info" title="Where to start">
If you are new to these, work through [Two Sum](/docs/arrays/two-sum), then [Best Time to Buy and Sell Stock](/docs/arrays/best-time-to-buy-and-sell-stock), then [Container With Most Water](/docs/arrays/container-with-most-water). Those three cover hash maps, running state and two pointers — and everything else builds on them.
</Callout>
